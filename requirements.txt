# Core data handling
pandas>=2.0

# Parquet IO (read_parquet / to_parquet)
pyarrow>=13.0

# Train/test split and stratification
scikit-learn>=1.3

# Hugging Face dataset/file access via hf:// URLs
fsspec>=2024.2.0
huggingface_hub>=0.21.0
